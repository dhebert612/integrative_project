events {
    worker_connections 1024;
}

http {
    # Rate limiting zones
    limit_req_zone $binary_remote_addr zone=general_limit:10m rate=10r/s;
    limit_conn_zone $binary_remote_addr zone=conn_limit:10m;

    # Shared security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # ============== HOMEPAGE ==============
    server {
        listen 8080;
        server_name home.davidhebert.site;

        limit_conn conn_limit 10;
        limit_req zone=general_limit burst=20 nodelay;

        location / {
            proxy_pass http://homepage:3001;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /api/ {
            proxy_pass http://homepage:3001/api/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Connection "";
            proxy_buffering off;
            proxy_cache off;
        }
    }

    # ============== PORTFOLIO ==============
    server {
        listen 8080 default_server;
        server_name davidhebert.site;

        limit_conn conn_limit 10;
        limit_req zone=general_limit burst=20 nodelay;

        location / {
            proxy_pass http://portfolio:80;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # ============== VAULTWARDEN ==============
    server {
        listen 8080;
        server_name vault.davidhebert.site;

        client_max_body_size 10M;
        limit_conn conn_limit 10;
        limit_req zone=general_limit burst=20 nodelay;

        location / {
            proxy_pass http://vaultwarden:80;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /notifications/hub {
            proxy_pass http://vaultwarden:80;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }

    # ============== DONETICK ==============
    server {
        listen 8080;
        server_name todo.davidhebert.site;

        limit_conn conn_limit 10;
        limit_req zone=general_limit burst=20 nodelay;

        location / {
            proxy_pass http://donetick:2021;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # ============== GRAFANA ==============
    server {
        listen 8080;
        server_name status.davidhebert.site;

        limit_conn conn_limit 10;
        limit_req zone=general_limit burst=20 nodelay;

        location / {
            proxy_pass http://grafana:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # ============== NGINX METRICS ==============
    server {
        listen 8081;
        server_name localhost;

        location /stub_status {
            stub_status;
        }
    }
}